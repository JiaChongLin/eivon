# Customer support bundle

This synthetic bundle demonstrates a second vertical domain. Install `examples.support_extension`, register a Python Tool with entrypoint `lookup_case`, and compose it into a Bundle or Agent. It has no dependency on FarmLynk or agriculture concepts.
